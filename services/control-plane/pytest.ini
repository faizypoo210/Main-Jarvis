[pytest]
testpaths = tests
asyncio_mode = auto
# One event loop per session — asyncpg + httpx ASGI must not hop loops (notably on Windows).
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
filterwarnings =
    ignore::DeprecationWarning

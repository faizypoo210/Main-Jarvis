# Copy to .env — never commit real secrets.
# Control plane is authoritative on port 8001 (not the voice server on 8000).
VITE_CONTROL_PLANE_URL=http://localhost:8001
VITE_CONTROL_PLANE_API_KEY=your-key-here
# Voice/WebSocket server (HTTP + WS); Command Center may derive WS URL from host + this port.
VITE_VOICE_SERVER_URL=http://localhost:8000

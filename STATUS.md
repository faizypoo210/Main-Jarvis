# Status ledger (honest, repo-grounded)

Categorization of **major surfaces** as they exist **today** in this repository. Status reflects **code + docs inspection**, not roadmap promises.

| Area | Status | Truth source | Note | Suggested verification |
|------|--------|--------------|------|-------------------------|
| **Control Plane** — missions, events, approvals, receipts, commands, bundle, health | **Implemented** | `services/control-plane/app/api/routes/`, PostgreSQL | Authoritative API for governed state | `GET /health`, `13-rehearse-golden-path.ps1` |
| **Control Plane** — operator read APIs (system health, usage, activity, integrations, **memory**, **heartbeat**) | **Implemented** | `app/api/routes/system.py`, `app/api/routes/operator.py`, `app/api/routes/operator_memory.py`, `app/api/routes/heartbeat.py` | Operator memory is **durable context** in `memory_items` — separate from mission rows and chat. **Heartbeat v1** stores deduped supervision rows in `heartbeat_findings` and exposes `GET /api/v1/operator/heartbeat`. **Approval reminders v1** (optional, env-gated) runs at the start of **`POST /api/v1/heartbeat/run`**, persists attempts in **`approval_reminders`**, optional SMS via existing Twilio + approval codes. **Cost guardrails v1** adds env-threshold `cost_*` findings from **`cost_events`** (same dedupe/resolve cycle; no invented budgets) | `GET /api/v1/operator/heartbeat`, `GET /api/v1/operator/cost-guardrails`, `POST /api/v1/heartbeat/run` (API key), `npm run build`, Activity → Heartbeat tab |
| **Control Plane** — worker registry + heartbeats (v1) | **Implemented** | `workers` table, `POST /api/v1/workers/register`, `POST /api/v1/workers/heartbeat` (API key), `GET /api/v1/operator/workers`, `system/health.worker_registry` | Core workers POST direct truth; supervision `stale_worker` uses `last_heartbeat_at` when present | `GET /api/v1/operator/workers`; migration `005_worker_registry`; Workers + System Health pages |
| **Control Plane** — SSE updates | **Implemented** | `app/api/routes/updates.py`, realtime hub | Stream + status endpoint | Command Center live badge; curl SSE as in benchmark docs |
| **Command Center** — overview, missions, mission detail, approvals | **Implemented** | `services/command-center/src/pages/` | **`/approvals`** loads **`GET /api/v1/approvals/{id}/bundle`** for structured review packets (list + detail). **Governed Action Launchers v1** on mission detail: typed forms POST existing **`/missions/{id}/integrations/github/*`** and **`/gmail/*`** routes (creates pending approval; **`requested_via: command_center`**). **`/approvals?approval=`** preselects a new request | `npm run build`; manual UI pass |
| **Command Center** — activity feed | **Implemented** | `pages/Activity.tsx`, `GET /api/v1/operator/activity` | Mission-event timeline; **Heartbeat** filter for open supervision findings | `npm run build`; hit `/activity` |
| **Operator Value Evals v1** | **Implemented** | `GET /api/v1/operator/evals`, `app/services/operator_value_evals.py`, `pages/Evals.tsx` | Bounded aggregates from missions/events/receipts/approvals/heartbeat/**cost_events** plus **`cost_guardrail_metrics`** (opened/resolved `cost_*` findings in window, open now) — **not** model-quality scoring | `npm run build`; `.\scripts\18-run-operator-value-evals.ps1` |
| **Control Plane** — cost events (v1) | **Implemented** | `cost_events` table (migration `006_cost_events_v1`), `record_cost_event_for_receipt`, `GET /api/v1/operator/cost-events` | **Direct / estimated / unknown / not_applicable**; no invented USD; linked to receipts when created | `GET /api/v1/operator/cost-events`; `alembic upgrade head` |
| **Command Center** — workers, cost & usage, system health | **Implemented** | `pages/*.tsx`, operator + system APIs | Cost & Usage shows **persisted cost truth** + **derived activity** volume + **guardrail status** (thresholds, breach flags, open/recent `cost_*` findings) | `npm run build` |
| **Command Center** — integrations | **Implemented** (honest) | `pages/Integrations.tsx`, `GET /api/v1/operator/integrations` | DB + safe machine/repo signals; **no** OAuth or vendor secrets in API | `npm run build`; curl integrations endpoint |
| **Coordinator** | **Implemented** | `coordinator/coordinator.py` | Stateless; DashClaw + Redis + control plane | Env set; run `09-start-coordinator.ps1`; observe logs |
| **Executor** | **Implemented** | `executor/executor.py` | OpenClaw + receipts | Live stack rehearsal doc |
| **Voice** | **Implemented** | `voice/server.py`, `voice/approval_voice.py`, `voice/briefing_voice.py`, `voice/governed_action_voice.py` | STT/TTS + `POST /api/v1/commands`; **Voice Approval v1**; **Voice briefing v1**; **Voice governed action requests v1** — narrow phrases to start GitHub/Gmail **approval requests** only (`requested_via: voice`, same integration POSTs as Command Center launchers); **read that again** repeats last voice reply | `voice/README.md`; `CONTROL_PLANE_API_KEY` for approval decisions + integration POSTs |
| **Approvals (end-to-end)** | **Implemented** | Control plane + UI + scripts + optional SMS | Shared schema `app/schemas/approvals.py`; **Approval Review Packets v1** — `GET /api/v1/approvals/{id}/bundle` + `approval_review_packet.py` (typed fields for governed GitHub/Gmail actions; merge preflight from mission events when present; **`reminder_summary`** from `approval_reminders`). **SMS approval v1** — Twilio outbound notify + `POST /api/v1/integrations/sms/inbound` with explicit `APPROVE`, `DENY`, or `READ` plus code only (`decided_via: sms`); migration `007_approval_sms_tokens`. **Reminder/escalation v1** — migration `008_approval_reminders`, governed mission events `approval_reminder_sent` / `approval_reminder_failed` / `approval_escalated` | Golden path script; `curl` bundle endpoint; `docs/SMS_APPROVALS.md` |
| **Benchmark / operator evals** | **Implemented** | `scripts/15-benchmark-operator-loop.ps1`, `docs/OPERATOR_EVALS.md` | Assumes control plane up; optional live stack | Run scripts; inspect `docs/reports/` output |
| **Model lanes** | **Implemented** (narrow, honest) | `docs/MODEL_LANES.md`, `shared/routing.py`, `shared/lane_truth.py`, executor/voice | **Mission routing** (`routing_decided`): requested vs actual mission path + `fallback_reason_code`; **executor** receipts: `openclaw_model_lane`, `lane_truth` (reconciled), `mission_execution_path`; voice direct Ollama is separate from mission path | `11-verify-model-lanes.ps1`, `11-smoke-model-lanes.ps1`; `GET /api/v1/operator/evals` routing section |
| **Integrations (vendor OAuth / Composio)** | **External / partial** | OpenClaw + Composio + machine | UI/API show **readiness only**; OAuth and keys stay outside repo | Vendor consoles + `DEPLOYMENT_STATUS.md` |
| **GitHub — governed issue + draft PR + PR merge** | **Implemented** (narrow) | `POST .../create-issue`, `.../create-pull-request`, `.../merge-pull-request`, `github_*_workflow.py`, `github_adapter.py` | Approval-gated (**red**); merge requires **preflight** (open, non-draft, mergeable, check runs); `JARVIS_GITHUB_TOKEN` | `docs/INTEGRATIONS_GITHUB.md` |
| **Gmail — governed create draft + reply draft + send draft** | **Implemented** (narrow) | `POST .../gmail/create-draft`, `POST .../gmail/create-reply-draft`, `POST .../gmail/send-draft`, `app/services/gmail_draft_workflow.py`, `app/integrations/gmail_adapter.py` | Approval-gated (`gmail_create_draft` / `gmail_create_reply_draft` / `gmail_send_draft`, risk **red**); OAuth on control plane (`JARVIS_GMAIL_*`); reply-draft uses **one** message id + metadata read (no inbox sync); send path is **existing draft only** (`drafts.send`); token needs **`gmail.send`** for send | `docs/INTEGRATIONS_GMAIL.md` |
| **OpenClaw workspace governance pack** | **Implemented** (tracked mirrors) | `config/workspace/` + `governance-manifest.json`, `docs/OPENCLAW_WORKSPACE_FILES.md` | SOUL / IDENTITY / USERS / AGENTS / MEMORY / HEARTBEAT / TOOLS — persona & policy for **local** OpenClaw; **not** mission authority (control plane) | `11-audit-workspace-governance.ps1`, `10-sync-openclaw-workspace.ps1` |
| **Operator memory (durable v1)** | **Implemented** | `app/models/memory_item.py`, Alembic `003_memory_items`, `memory_promotion.py` | Manual + explicit mission promotion + structured `memory_candidate` on receipts only; **no** vector DB, **no** auto-ingest of missions/commands/logs | `alembic upgrade head`; POST with API key; receipt payload shape in `memory_promotion.py` |
| **Security / governance docs** | **Implemented** (docs) | `docs/SECURITY_REVIEW.md`, etc. | Security/trust separate from workspace persona files | — |
| **Deployment / smoke automation** | **Implemented** (scoped) | `DEPLOYMENT_STATUS.md`, `08-final-report.ps1`, `08-smoke-operator-control-plane.ps1`, `08-smoke-external-probes.ps1` | **Core** = infra core + gateway + LAN + operator API smoke + workspace governance audit; extended infra + OAuth probes optional (skip if no token) | `08-final-report.ps1`; live pipeline still `09-smoke-test-e2e.ps1` / `14-rehearse-live-stack.ps1` |

## Legend

- **Implemented**: Usable end-to-end for the stated scope in a correctly configured dev environment.
- **Partial**: Substantial code exists but gaps (missing routes, incomplete UI, or environment-only behavior).
- **Placeholder**: Intentional stub; not a hidden production feature.
- **External / Machine-local**: Depends on installs, secrets, or repos outside this clone.
- **Planned**: Reserved for items explicitly tracked in roadmap docs—not used here without a doc reference.
